{
  "metadata": {
    "id": 128153862,
    "ref": "busyaprime/test-your-agent-a-local-matchup-harness",
    "title": "Test your agent a local matchup harness",
    "author": "Busya PRIME",
    "slug": "test-your-agent-a-local-matchup-harness",
    "lastRunTime": "2026-08-04T18:50:12.510197Z",
    "language": "python",
    "kernelType": "notebook",
    "isPrivate": false,
    "enableGpu": false,
    "enableInternet": false,
    "competitionDataSources": [
      "pokemon-tcg-ai-battle"
    ],
    "totalVotes": 3,
    "enableTpu": false,
    "currentVersionNumber": 2,
    "dockerImage": "gcr.io/kaggle-images/python@sha256:dafd4ce5668bbf1ad422e4c109e0f18c9623c3a7c7f48b0235f13142755c40b9",
    "machineShape": "None"
  },
  "blob": {
    "source": "{\"cells\":[{\"cell_type\":\"markdown\",\"id\":\"bd4ad572\",\"metadata\":{},\"source\":\"# Test your agent before you submit: a local matchup harness\\n\\nYou get a handful of ladder submissions a day, and the leaderboard only tells you a converged rating after\\nmany games. That is a slow loop for trying a new deck or a new decision rule. This notebook is the fast\\nloop: a small, dependency free harness that runs full games between any two agents on the competition's own\\nengine, right here on CPU, and reports win rates in seconds.\\n\\nIt ships with a transparent rule based baseline so the harness has something to run out of the box. The\\nbaseline is deliberately simple and it is meant to be beaten, it is a floor to measure your ideas against,\\nnot a top ladder agent. Everything here is honest: the numbers below are produced by the code in the\\nnotebook, and there is no leaderboard figure anywhere because a local harness cannot produce one.\"},{\"cell_type\":\"code\",\"execution_count\":null,\"id\":\"dba155c7\",\"metadata\":{},\"outputs\":[],\"source\":\"import glob, os, sys, importlib, types\\ndef find_dir(pats, must):\\n    for p in pats:\\n        for d in glob.glob(p, recursive=True):\\n            if os.path.isdir(d) and os.path.exists(os.path.join(d, must)):\\n                return d\\n    return None\\nSAMPLE = find_dir([\\\"/kaggle/input/**/sample_submission/**\\\", \\\"/kaggle/input/**/sample_submission\\\"], \\\"main.py\\\")\\nCG = find_dir([\\\"/kaggle/input/**/sample_submission/**/cg\\\", \\\"/kaggle/input/**/cg\\\"], \\\"api.py\\\")\\nsys.path.insert(0, os.path.dirname(CG))\\nfrom cg.api import to_observation_class, all_card_data, all_attack\\nfrom cg.game import battle_start, battle_select, battle_finish\\nSAMPLE_DECK = [int(x) for x in open(os.path.join(SAMPLE, \\\"deck.csv\\\")).read().splitlines() if x.strip()]\\nprint(\\\"engine ready, sample deck has\\\", len(SAMPLE_DECK), \\\"cards\\\")\"},{\"cell_type\":\"markdown\",\"id\":\"d09e835c\",\"metadata\":{},\"source\":\"## The harness\\n\\nAn agent is a function `agent(obs_dict) -> list[int]`. On the very first call the observation has no\\nselection and the agent returns its 60 card deck; after that the engine hands it a list of legal options\\neach turn and the agent returns the indices it picks. `play` runs one full game between two agents, routing\\neach decision to whichever agent is on the move, and `matchup` runs a batch with the seats alternated so a\\nfirst player advantage does not bias the number. The engine keeps one battle at a time, so games run in\\nsequence and `battle_finish` is called before the next one.\"},{\"cell_type\":\"code\",\"execution_count\":null,\"id\":\"3c188a74\",\"metadata\":{},\"outputs\":[],\"source\":\"def play(agent_a, deck_a, agent_b, deck_b, a_first=True):\\n    d0, d1 = (deck_a, deck_b) if a_first else (deck_b, deck_a)\\n    obs, start = battle_start(d0, d1)\\n    if obs is None:\\n        return {\\\"outcome\\\": \\\"deck_rejected\\\", \\\"errorType\\\": getattr(start, \\\"errorType\\\", None)}\\n    a_seat = 0 if a_first else 1\\n    try:\\n        steps = 0\\n        while obs[\\\"current\\\"][\\\"result\\\"] < 0 and steps < 100000:\\n            seat = obs[\\\"current\\\"][\\\"yourIndex\\\"]\\n            move = (agent_a if seat == a_seat else agent_b)(obs)\\n            obs = battle_select(move)\\n            steps += 1\\n        res = obs[\\\"current\\\"][\\\"result\\\"]        # -1 ongoing, 0 or 1 winner seat, 2 draw\\n        return {\\\"outcome\\\": \\\"done\\\", \\\"a_win\\\": res == a_seat, \\\"draw\\\": res == 2, \\\"turns\\\": obs[\\\"current\\\"][\\\"turn\\\"]}\\n    except Exception as e:\\n        return {\\\"outcome\\\": \\\"error\\\", \\\"error\\\": type(e).__name__}     # an illegal index throws, and that loses\\n    finally:\\n        battle_finish()\\n\\ndef matchup(agent_a, deck_a, agent_b, deck_b, n=60):\\n    wins = draws = errors = done = 0; turns = []\\n    for g in range(n):\\n        r = play(agent_a, deck_a, agent_b, deck_b, a_first=(g % 2 == 0))\\n        if r[\\\"outcome\\\"] != \\\"done\\\":\\n            errors += 1; continue\\n        done += 1; turns.append(r[\\\"turns\\\"])\\n        draws += r[\\\"draw\\\"]; wins += (r[\\\"a_win\\\"] and not r[\\\"draw\\\"])\\n    return {\\\"games\\\": done, \\\"win_rate\\\": round(wins / max(1, done), 3), \\\"draws\\\": draws,\\n            \\\"errors\\\": errors, \\\"avg_turns\\\": round(sum(turns) / max(1, len(turns)), 1)}\\nprint(\\\"harness ready\\\")\"},{\"cell_type\":\"markdown\",\"id\":\"97949323\",\"metadata\":{},\"source\":\"## The baseline agent\\n\\nThe engine only ever offers legal options, so a rule based agent is just a way to score those options. This\\none scores by option type: use free abilities, evolve, attach the turn's energy and play cards first, then\\nattack, and only end the turn if nothing else is worth doing. For attacks it looks up the real damage from\\nthe card data and jumps on any attack that knocks out the defender, taking weakness into account. It never\\nreturns an illegal index, so it always finishes a game. It is short on purpose. Fork it and make it smarter.\"},{\"cell_type\":\"code\",\"execution_count\":null,\"id\":\"afbd93a2\",\"metadata\":{},\"outputs\":[],\"source\":\"_C = {c.cardId: c for c in all_card_data()}\\n_A = {a.attackId: a for a in all_attack()}\\nPLAY, ATTACH, EVOLVE, ABILITY, DISCARD, RETREAT, ATTACK, END, SKILL, NUMBER = 7, 8, 9, 10, 11, 12, 13, 14, 15, 0\\nMAIN, ACTIVE = 0, 4\\n\\ndef _damage(attack_id, attacker, defender):\\n    a = _A.get(attack_id)\\n    if a is None:\\n        return 0\\n    d = a.damage or 0\\n    if attacker is not None and defender is not None:\\n        atype = getattr(_C.get(attacker.id), \\\"energyType\\\", None)\\n        weak = getattr(_C.get(defender.id), \\\"weakness\\\", None)\\n        res = getattr(_C.get(defender.id), \\\"resistance\\\", None)\\n        if atype is not None and weak is not None and int(weak) == int(atype):\\n            d *= 2\\n        elif atype is not None and res is not None and int(res) == int(atype):\\n            d = max(0, d - 30)\\n    return d\\n\\ndef make_baseline(deck):\\n    def agent(obs_dict):\\n        obs = to_observation_class(obs_dict)\\n        sel = obs.select\\n        if sel is None:\\n            return deck\\n        opts = sel.option; n = len(opts)\\n        lo = int(getattr(sel, \\\"minCount\\\", 1) or 0); hi = int(getattr(sel, \\\"maxCount\\\", 1) or 1)\\n        try:\\n            if int(sel.type) == MAIN:\\n                st = obs.current\\n                me = st.players[st.yourIndex]; opp = st.players[1 - st.yourIndex]\\n                my_active = me.active[0] if me.active and me.active[0] else None\\n                opp_active = opp.active[0] if opp.active and opp.active[0] else None\\n                scores = []\\n                for op in opts:\\n                    t = int(op.type)\\n                    if t == ATTACK:\\n                        dmg = _damage(op.attackId, my_active, opp_active)\\n                        s = (1000 + dmg) if (opp_active is not None and dmg >= (opp_active.hp or 9999)) else 30 + min(dmg, 250) / 10.0\\n                    elif t == ABILITY: s = 62\\n                    elif t == EVOLVE: s = 58 if (op.inPlayArea is not None and int(op.inPlayArea) == ACTIVE) else 55\\n                    elif t == ATTACH: s = 52 if (op.inPlayArea is not None and int(op.inPlayArea) == ACTIVE) else 46\\n                    elif t == PLAY: s = 40\\n                    elif t == SKILL: s = 25\\n                    elif t == RETREAT: s = 8\\n                    elif t == DISCARD: s = 4\\n                    elif t == END: s = 0\\n                    else: s = 20\\n                    scores.append(s)\\n                return [max(range(n), key=lambda i: scores[i])]\\n            if n and all(int(o.type) == NUMBER for o in opts):\\n                vals = [(o.number if o.number is not None else 0) for o in opts]\\n                k = min(max(lo, 1), hi, n)\\n                return sorted(sorted(range(n), key=lambda i: -vals[i])[:k])\\n            k = min(max(lo, 1), hi, n) if hi >= 1 else 0\\n            return list(range(k))\\n        except Exception:\\n            k = min(max(lo, 1), hi, n) if (n and hi >= 1) else 0\\n            return list(range(k)) if k else ([0] if n else [])\\n    return agent\\nprint(\\\"baseline ready\\\")\"},{\"cell_type\":\"markdown\",\"id\":\"7ac90065\",\"metadata\":{},\"source\":\"## Run it\\n\\nBelow the baseline plays the competition's own sample agent, which picks legal options at random, and then\\na mirror of itself. The baseline should win comfortably against random play and sit near half against its\\nown mirror, which is exactly what a sane harness and a sane floor look like. Swap in your own agent and\\ndeck and you have your local test bench.\"},{\"cell_type\":\"code\",\"execution_count\":null,\"id\":\"59bf580e\",\"metadata\":{},\"outputs\":[],\"source\":\"sys.path.insert(0, SAMPLE)\\nrandom_sample = importlib.import_module(\\\"main\\\").agent    # the official random sample agent\\nbaseline = make_baseline(SAMPLE_DECK)\\n\\nprint(\\\"baseline vs the random sample :\\\", matchup(baseline, SAMPLE_DECK, random_sample, SAMPLE_DECK, n=60))\\nprint(\\\"baseline vs a mirror of itself:\\\", matchup(baseline, SAMPLE_DECK, make_baseline(SAMPLE_DECK), SAMPLE_DECK, n=30))\"},{\"cell_type\":\"markdown\",\"id\":\"066f30b6\",\"metadata\":{},\"source\":\"## Using it on your own work\\n\\nPoint `matchup` at your agent and any opponent:\\n\\n```python\\nyour_agent = make_your_agent(your_deck)       # any agent(obs_dict) -> list[int]\\nprint(matchup(your_agent, your_deck, opponent_agent, opponent_deck, n=100))\\n```\\n\\nTo measure against the strong published agents rather than random play, fork one of the public sample\\nkernels (the rule based Mega Lucario ex, Dragapult ex, Iono and Mega Abomasnow ex decks, and the\\nreinforcement learning sample), lift its `agent` function and deck, and pass them in as the opponent. Run a\\nfew hundred games per opponent and alternate seats, as `matchup` already does, before you trust a number.\\n\\nTwo things the harness makes easy to see. An agent that ever returns an illegal option index throws, and\\nthat counts as a loss, so the `errors` field being zero is the first thing to check on a new agent. And\\nmargin of victory does not matter on the ladder, only the win, so tune for winning more games, not for\\nwinning them faster.\\n\\nIf you want to know which decks are actually worth testing against right now, the companion notebook\\n[What actually wins on the ladder](https://www.kaggle.com/code/busyaprime/what-actually-wins-on-the-ladder)\\nrecomputes the current deck tier list and matchup grid from the day's battle logs.\"}],\"metadata\":{\"kernelspec\":{\"display_name\":\"Python 3\",\"language\":\"python\",\"name\":\"python3\"},\"language_info\":{\"name\":\"python\"}},\"nbformat\":4,\"nbformat_minor\":5}",
    "language": "python",
    "kernelType": "notebook",
    "slug": "test-your-agent-a-local-matchup-harness"
  }
}